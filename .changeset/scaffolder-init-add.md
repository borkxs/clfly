---
"@clfly/cli": minor
"@clfly/create": minor
"@clfly/core": patch
---

Dogfood `clfly` as a real command tree: `init` / `add` / `build` / `completions`, thin `create-clfly` alias, core library-only (no bin). Tool-name collisions hard-fail; root index excluded from MCP projection.
